# Simple CRUD Fullstack Javascript
## How to run?

First git clone this app, and then :
```
npm i
```
inside folder `client` and `server`, and type :
```
node index.js
```
inside folder server and :
```
npm start 
```
inside client folder :)